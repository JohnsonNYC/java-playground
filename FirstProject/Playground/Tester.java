package FirstProject.Playground;
import java.util.*;
public class Tester {
  public static void main(String[] args) {
    int[] tempList = {1, -1, -2};
    int[] numList = {-2,-1,1};
    int n = 2;
    int k = 0;
    System.out.println(n/k);

  }
}


// Given String input
    // Rever the number
    // get the sume of input and reversed number
    // check if sum is a palindrome
    // if it's not, repeat the process
    // String input = "124";
    // String reversedInput = palindrome(input);
    // int sum = Integer.valueOf(input) + Integer.valueOf(reversedInput);

    // while(!String.valueOf(sum).equals(String.valueOf(palindrome(String.valueOf(sum))))){
    //   input = String.valueOf(sum);
    //   reversedInput = palindrome(input);
    //   sum = Integer.valueOf(input) + Integer.valueOf(reversedInput);
    // }

    // System.out.println(sum);

     // public static String palindrome(String input){
  //   String result = "";
  //   for(int i = input.length() - 1; i >=0; i--){
  //     result += input.charAt(i);
  //   }
  //   return result;
  // }